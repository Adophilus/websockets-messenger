{
  "name": "websockets-messenger",
  "version": "1.0.0",
  "description": "A project for demoing my research on websockets",
  "scripts": {
    "build:client": "cd client && pnpm build",
    "build:server": "cd server && pnpm build",
    "build": "pnpm build:client && pnpm build:server && mv ./client/build ./server/frontend",
    "start": "cd server && pnpm start",
    "dev": "node dev"
  },
  "keywords": [],
  "author": "Adophilus",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
